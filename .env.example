# Copy this file to .env, then replace the API key locally.
# The .env file is ignored by git and loaded automatically by the query CLI.
LLM_PROVIDER=openai
OPENAI_API_KEY=replace-me
OPENAI_MODEL=gpt-5-mini
OPENAI_API_BASE=https://api.openai.com
